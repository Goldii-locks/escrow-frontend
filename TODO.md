# TODO
- [ ] Update `package.json`: add `@storybook/react` and `@storybook/blocks` to `dependencies`.
- [ ] Update `tsconfig.json`: exclude `**/*.stories.tsx` from TypeScript compilation/type-check.
- [ ] Run `npm install` (or `npm ci` if lockfile is used) and verify `npm run build` / type-check passes.

